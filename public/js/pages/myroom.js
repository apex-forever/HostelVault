/* ===========================================================
   FILE: js/pages/myroom.js — Student's room view
   =========================================================== */

async function renderMyRoom() {
  const [rooms, blocks, wardens] = await Promise.all([API.get('/rooms'), API.get('/blocks'), API.get('/wardens')]);
  const room = rooms.find(r => r.id === currentUser.roomId);
  const block = room ? blocks.find(b => b.id === room.blockId) : null;
  const mates = room ? room.occupants : [];
  const blockWardens = block ? wardens.filter(w => w.blockId === block.id && w.status === 'active') : [];
  const wardenLabel = blockWardens.length ? blockWardens.map(w => w.name).join(', ') : 'Unassigned';
  const pc = document.getElementById('pageContent');
  if (!room) { pc.innerHTML = '<div class="empty-state fade-up"><i class="fas fa-door-open"></i><p>No room assigned yet</p></div>'; return; }
  pc.innerHTML = `<div class="grid grid-cols-1 lg:grid-cols-3 gap-5 fade-up"><div class="content-card lg:col-span-2"><div class="card-header"><h3 class="card-title">Room ${room.id}</h3>${getStatusBadge(room.status)}</div><div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-4"><div class="text-center p-3 rounded-lg" style="background:var(--bg)"><div class="font-display font-bold text-lg">${room.type}</div><div class="text-xs" style="color:var(--text-muted)">Type</div></div><div class="text-center p-3 rounded-lg" style="background:var(--bg)"><div class="font-display font-bold text-lg">${room.capacity}</div><div class="text-xs" style="color:var(--text-muted)">Capacity</div></div><div class="text-center p-3 rounded-lg" style="background:var(--bg)"><div class="font-display font-bold text-lg">Floor ${room.floor}</div><div class="text-xs" style="color:var(--text-muted)">Floor</div></div><div class="text-center p-3 rounded-lg" style="background:var(--bg)"><div class="font-display font-bold text-lg">${formatCurrency(room.price)}</div><div class="text-xs" style="color:var(--text-muted)">Per Semester</div></div></div><h4 class="font-semibold mb-3">Roommates</h4>${mates.map(m => `<div class="flex items-center gap-3 py-3 border-b" style="border-color:var(--border)"><div style="width:40px;height:40px;border-radius:50%;background:var(--primary-pale);display:flex;align-items:center;justify-content:center;font-weight:700;font-size:14px;color:var(--primary)">${m.name.split(' ').map(w => w[0]).join('').substring(0, 2).toUpperCase()}</div><div><p class="font-semibold text-sm">${m.name}${m.id === currentUser.id ? ' (You)' : ''}</p></div></div>`).join('')}</div><div class="content-card"><h3 class="card-title mb-4">Block Info</h3>${block ? `<p class="font-semibold mb-1">${block.name}</p><p class="text-sm mb-3" style="color:var(--text-muted)">${block.description}</p><div class="text-sm mb-2"><i class="fas fa-venus-mars mr-2" style="color:var(--primary-lighter)"></i>${block.type} Hostel</div><div class="text-sm mb-2"><i class="fas fa-layer-group mr-2" style="color:var(--primary-lighter)"></i>${block.floors} Floors</div><div class="text-sm"><i class="fas fa-user-shield mr-2" style="color:var(--primary-lighter)"></i>${wardenLabel}</div>` : ''}</div></div>`;
}
